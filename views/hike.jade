extends layout

block content
  .container
    h1= title
    p Welcome to #{title}
    p test continuous delivery end-to-end
    p still need a set of tests to run between pipeline and beanstalk
    p github to code pipeline to beanstalk to ec2 to express